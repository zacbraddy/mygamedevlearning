import React from 'react'

export default ({ children }) => (
  <div className="p-2 mx-4 text-sm font-bold border-2 rounded-full bg-primary-200 text-primary-400 border-tertiary-400">
    {children}
  </div>
)
